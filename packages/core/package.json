{
  "name": "@clash-tracker/core",
  "version": "0.1.0",
  "private": true,
  "description": "Pure domain logic for Clash Tracker — ranking, stats medians, eligibility, CWL planning. No I/O, no Firebase.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "build": "tsc -p tsconfig.json",
    "lint": "eslint . --no-error-on-unmatched-pattern",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^4.1.8",
    "vitest": "^4.1.8"
  }
}
