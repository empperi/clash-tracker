# Enforce LF line endings for all text files
* text=auto eol=lf

# Treat images as binary: no EOL normalization, and NOT via Git LFS — they are small app
# assets, so plain Git avoids LFS quota/CI-checkout pitfalls (a broken icon breaks PWA install).
*.png binary
*.jpg binary
*.gif binary
*.ico binary
